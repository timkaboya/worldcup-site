{
  "name": "worldcup-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "World Cup 2026 Companion — lightweight, timezone-correct, no-accounts web app.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "preview:cf": "astro build && wrangler pages dev dist",
    "check": "astro check",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "perf": "node scripts/check-bundle.mjs",
    "e2e": "playwright test"
  },
  "dependencies": {
    "@astrojs/preact": "^4.0.1",
    "astro": "^4.16.0",
    "preact": "^10.24.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@playwright/test": "^1.61.1",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
